# New Book

```julia (editor=true, logging=true, output=true)
using Bonito
using Pkg;Pkg.activate(".")
using MonteCarloHealth
```
```julia (editor=true, logging=false, output=true)
using Bonito: @bind, Slider, Checkbox, Select, @react

```
